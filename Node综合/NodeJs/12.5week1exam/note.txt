要求：实现文件拷贝效果（包括大文件和小文件）
//-----思路与要求：
    1.创建文件夹进行读取下面的所有文件
    2.文件夹内至少包括一个大型mp4文件和一个小文件
    3.读取到文件之后进行判断
    4.如果文件大于1mb则使用数据流读取
    5.如果文件小于1mb则使用fs.readfile读取
    6.判断读取的文件是什么后缀名，输出的时候就正确输出获取的后缀名
    7.将读取的文件正常输出到另一个文件夹就行

    //判断文件大小：stats.size得到的是文件的字节
    1MB=1024KB
    1KB=1024B[字节]